{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "flowpro-web",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "export NVM_DIR=\"$HOME/.nvm\" && [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\" && nvm use 20.19.6 && cd web && npx vite --port $PORT"],
      "port": 5180,
      "autoPort": true
    }
  ]
}
